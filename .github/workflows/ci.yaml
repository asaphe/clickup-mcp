name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true

      - name: Install
        run: uv sync --all-groups

      - name: Lint
        run: |
          set -euo pipefail
          uv run ruff check .
          uv run ruff format --check .

      - name: Type check
        run: |
          set -euo pipefail
          uv run mypy clickup_mcp_server

      - name: Test
        run: |
          set -euo pipefail
          uv run pytest

      - name: Dependency hygiene
        run: |
          set -euo pipefail
          uv run deptry .

      - name: Fixture hygiene
        run: |
          set -euo pipefail
          python3 .github/scripts/check_fixture_ids.py
